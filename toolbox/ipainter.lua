--- @meta
--- Provides access to the painter tool's properties.
--- 
--- Only available if the builder tool's ID is 3.
--- @class IPainter
--- @field IsPainting boolean `true` if the painter tool is painting something; otherwise, `false`.
--- @field IsRemoving boolean `true` if the painter tool is removing paint; otherwise, `false`.
local IPainter = {}
